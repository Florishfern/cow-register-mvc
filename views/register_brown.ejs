<%- include('partials/header') %>

<div class="card shadow-sm">
  <div class="card-body">
    <div class="d-flex justify-content-between align-items-center">
      <h2 class="h5 mb-0">2) ลงทะเบียนวัวสีน้ำตาล (Brown)</h2>
      <a href="/" class="btn btn-sm btn-outline-secondary">กลับหน้าเลือกสี</a>
    </div>

    <% if (error) { %>
      <div class="alert alert-danger mt-3"><%- error %></div>
    <% } %>

    <form class="mt-3" method="POST" action="/register/brown">
      <div class="row">
        <div class="col-md-6 mb-3">
          <label class="form-label">cow_id (8 หลัก, หลักแรกไม่ใช่ 0)</label>
          <input class="form-control" name="cow_id" maxlength="8" value="<%= (data.cow_id || '') %>" required>
        </div>
        <div class="col-md-6 mb-3">
          <label class="form-label">farm_id (1-9, 1 หลัก, ไม่ใช่ 0)</label>
          <input class="form-control" name="farm_id" maxlength="1" value="<%= (data.farm_id || '') %>" required>
        </div>
      </div><div class="mb-3">
        <label class="form-label">mother_id (รหัสแม่พันธุ์: cow_id ของวัวตัวอื่น)</label>
        <input class="form-control" name="mother_id" maxlength="8" value="<%= (data.mother_id || '') %>" required>
        <div class="form-text">ต้องมีอยู่ในฐานข้อมูลก่อน (ลงทะเบียนแม่ก่อนลูก)</div>
      </div>
      <button class="btn btn-success" type="submit">ลงทะเบียน</button>
    </form>

    <div class="mt-3 text-muted">
      หมายเหตุ: ฟาร์ม 1-9 รับวัวได้แค่ “สีเดียว” (ถ้ามีสีอื่นอยู่แล้วจะลงทะเบียนไม่ได้)
    </div>
  </div>
</div>

<%- include('partials/footer') %>
