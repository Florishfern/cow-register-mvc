<%- include('partials/header') %>

<div class="card shadow-sm">
  <div class="card-body">
    <div class="d-flex justify-content-between align-items-center">
      <h2 class="h5 mb-0">3) สรุปจำนวนวัวและสีของวัวในแต่ละฟาร์ม</h2>
      <a href="/" class="btn btn-sm btn-primary">กลับหน้าเลือกสี</a>
    </div>

    <% if (message) { %>
      <div class="alert alert-success mt-3"><%- message %></div>
    <% } %>
    <% if (error) { %>
      <div class="alert alert-danger mt-3"><%- error %></div>
    <% } %>

    <div class="table-responsive mt-3">
      <table class="table table-striped align-middle">
        <thead>
          <tr>
            <th>farm_id</th>
            <th>farm_color (สีที่ฟาร์มรับ)</th>
            <th>cow_count</th>
          </tr>
        </thead>
        <tbody>
          <% summary.forEach(row => { %>
            <tr>
              <td><%= row.farm_id %></td>
              <td><%= row.farm_color || '-' %></td>
              <td><%= row.cow_count %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>

    <div class="text-muted">ถ้าฟาร์มยังไม่มีวัว ระบบจะแสดง farm_color เป็น “-”</div>
  </div>
</div>

<%- include('partials/footer') %>
